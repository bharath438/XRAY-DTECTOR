# XRAY-DTECTOR
The X-ray Detection Streamlit app involves two primary actors: the User (such as a doctor) and the AI System. The user can upload chest X-ray images in formats like .jpg or .png to the system. Once an image is uploaded, the AI model processes it 
